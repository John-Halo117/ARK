# Lint + test toolchain and all worker runtime deps (single venv for CI/local)
ruff>=0.8
pytest>=8
pytest-asyncio>=0.24
pytest-cov>=5

duckdb==1.1.3
nats-py==2.9.0
pandas==2.2.3
pyarrow==17.0.0
docker==7.1.0
requests==2.32.3
